# Core data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0

# S3 / cloud storage
s3fs>=2024.2.0
fsspec>=2024.2.0
boto3>=1.34.0

# Distributed / large data (pivot_utils)
dask[dataframe]>=2024.1.0

# Plotting (PCA variance explained)
matplotlib>=3.7.0

# CLI and progress
tqdm>=4.66.0

# Fallback for Parquet codec issues (e.g. GZipCodec failed); optional
fastparquet>=2024.1.0

# Testing (optional)
pytest>=7.4.0

# Optional
pytest-cov>=4.1.0
pytest-sugar>=0.9.7
